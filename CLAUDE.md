# CLAUDE.md

本文件是给 Claude Code 的项目说明，每次会话启动时自动加载。

## 仓库用途

这是 huifeng.yan 的**个人工作记录仓库**（`work_record`），用于沉淀日常工作文档、项目笔记和 SDK 资料，不是代码工程项目。内容以 Markdown 和 Word 文档为主，提交的是文档而非源码。

## 目录结构

- `AI_SDK/` — AI SDK / TDL SDK 相关资料（开发环境搭建指南、开发规范、初始项目介绍）
- `无人机项目/` — 无人机项目的初始代码、问题迭代记录、AI 求解笔记
- `月周日工作记录/` — 按月归档的工作记录（如 `2026.7月.md`）
- `.claude/skills/` — 项目级 Claude Code skills

文件名含中文，git 输出里会显示成转义八进制（如 `\346\223\215...`），这是正常的，不要去"修复"。

## 提交规范（重要）

本仓库的提交由 Claude 协助完成，遵循以下约定：

- **提交信息用中文**，动词开头（新增 / 整理 / 更新 / 修复 / 补充），概括内容、不罗列文件名，≤30 字，**不加句号**，**不加 `Co-Authored-By` 尾注**（这是个人记录仓库）。
- 参考已有提交：
  - `整理文档：重命名并补充无人机项目迭代记录`
  - `新增TDL_SDK开发指南/规范及AI求解文档`
  - `新增git-push skill：一键暂存提交推送`
- 默认流程：`git add -A && git commit -m "<信息>" && git push`，合并为一条命令。
- 提交者身份是自动推断的 `huifeng.yan@sophgo-inc.com`，无需处理；除非用户主动要求修正身份。
- 工作区干净时不要创建空提交。

## 可用 Skills

- `git-push` — 一键暂存、提交并推送当前改动。当用户说"推送一下"、"提交一下"、"git推送"、"帮我提交"、"在提交"等时触发。详见 `.claude/skills/git-push/SKILL.md`。

## 工作风格

- 用户主要用中文交流，回复也用中文。
- 文档操作为主，遇到中文文件名正常处理，不要重命名为英文。
- 推送失败（网络/鉴权）时原样反馈报错，不反复重试。
- 不要主动改动文档内容，除非用户明确要求编辑某文件。
